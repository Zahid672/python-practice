###Merge and sort: Write a program that merges two lists and sorts the resulting list in ascending order.

first_list = [3, 2, 1, 5]

second_list = [6, 7, 10, 9]

merge_list = first_list + second_list
print(merge_list)
print(sorted(merge_list))