### Deleting a File using the OS module: Module (like a code library (pre-install)) is a file written by another programmer that generally has a functions we can use.
import os

os.remove("sample.txt")